export interface ReturnMessage {
    rowCounter:          number;
    updateState:         boolean;
    message:             string;
}

export interface KeyValue {
    name:          string;
    value:         number;
}